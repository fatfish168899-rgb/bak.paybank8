document.addEventListener('DOMContentLoaded', function () {
    // 设置页面交互逻辑 (如有需要可以扩展为 AJAX)
    const settingsForm = document.querySelector('form');
    if (settingsForm) {
        settingsForm.addEventListener('submit', function () {
            // 这里可以添加提交前的 UI 反馈
        });
    }
});
